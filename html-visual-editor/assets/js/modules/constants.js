/**
 * Seletor do container renderizado pelo Widget HTML do Elementor.
 * Leitura pública do DOM gerado pelo próprio Elementor — nenhum código
 * de terceiros é reutilizado, apenas o marcador padrão da plataforma
 * para localizar onde o HTML do usuário foi impresso.
 *
 * Compartilhado entre Editor e DomWatcher para nunca ficar duplicado.
 */
export const WIDGET_CONTAINER_SELECTOR = '.elementor-widget-html .elementor-widget-container';
